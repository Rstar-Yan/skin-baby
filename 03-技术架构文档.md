# 肤小宝 — 技术架构文档

---

## 一、整体架构

```
┌─────────────────────────────────────┐
│           微信小程序前端              │
│    (uni-app / Vue3)                  │
└──────────────┬──────────────────────┘
               │ HTTPS
┌──────────────▼──────────────────────┐
│         API Gateway (Nginx)          │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│         业务服务层 (Node.js/Go)       │
│  ┌────────┐ ┌──────┐ ┌──────────┐  │
│  │用户服务│ │记录服务│ │推送服务  │  │
│  └────────┘ └──────┘ └──────────┘  │
│  ┌────────┐ ┌──────┐ ┌──────────┐  │
│  │知识库  │ │分析服务│ │AI 服务   │  │
│  └────────┘ └──────┘ └──────────┘  │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│           数据层                      │
│  MySQL (业务数据) + COS (图片)        │
│  Redis (缓存/队列)                    │
└─────────────────────────────────────┘
```

---

## 二、技术栈推荐

| 层级 | 推荐方案 | 理由 |
|------|----------|------|
| 小程序框架 | **uni-app**（Vue3） | 一套代码可编译微信/支付宝/百度多端，生态成熟 |
| UI 组件库 | **uView Plus** 或 **uni-ui** | 开箱即用，适合 MVP 快速出活 |
| 后端语言 | **Node.js (NestJS)** 或 **Go (Gin)** | 个人开发者推荐 Node.js，全栈统一语言；追求性能选 Go |
| 数据库 | **MySQL 8.0**（云数据库） | 关系型，记录和统计查询适合 |
| 图片存储 | **腾讯云 COS** | 与微信生态天然集成，价格低 |
| 推送 | **微信订阅消息** | 合规推送，一次订阅可推一次，需要设计"每次完成润肤后引导重新订阅" |
| AI 皮肤评估 | **腾讯云 AI 皮肤病检测** 或自建 | 腾讯云 API 现成可用；长远可基于用户照片自训练 |
| 部署 | **微信云托管** | 免运维，与小程序内网互通，免域名备案 |

---

## 三、数据模型（核心表）

```sql
users
  id, openid, nickname, avatar, child_name, child_birthday, 
  child_gender, diagnosis_date, created_at

moisturize_logs
  id, user_id, applied_at, product_name, body_areas, note

medication_logs
  id, user_id, drug_id, applied_at, dosage, areas

drugs
  id, user_id, name, type(steroid/non_steroid/moisturizer), 
  strength, is_active

symptom_records
  id, user_id, recorded_at, itch_score(0-10), 
  photo_urls(JSON), body_areas(JSON), severity_self_assess, note

trigger_logs
  id, user_id, occurred_at, category, detail, symptom_record_id

push_subscriptions
  id, user_id, type(moisturizer/drug), time_slots(JSON), 
  is_active, template_id
```

---

## 四、开发周期估算（单人全职）

| 阶段 | 内容 | 工期 |
|------|------|------|
| 第 1 周 | 项目搭建 + 用户登录 + 基础框架 | 1 周 |
| 第 2~3 周 | 润肤提醒 + 用药管理 + 推送 | 2 周 |
| 第 4~5 周 | 症状日记（拍照+评分+部位标注+趋势图） | 2 周 |
| 第 6 周 | 诱因日志 + 知识库（静态内容） | 1 周 |
| 第 7 周 | 整体联调 + bug 修复 + 体验打磨 | 1 周 |
| 第 8 周 | 内测 + 医学审核 + 提审上线 | 1 周 |

MVP 总计约 **2 个月**。

---

## 五、合规注意事项

| 风险 | 说明 | 缓解策略 |
|------|------|-----------|
| **医疗资质红线** | 若提供诊断/治疗建议，可能被认定为医疗器械（需 NMPA 注册） | 明确定位为「健康管理/养护辅助工具」，标注"不替代医生诊断" |
| **隐私合规** | 涉及儿童健康数据，须遵守《个人信息保护法》及儿童数据特殊保护 | 数据最小化采集，不出售数据，尽早做等保 |
| **内容审核** | 医学内容需专业审核 | 找 1~2 位皮肤科医生做内容审核顾问 |